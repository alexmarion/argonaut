export const ACTION_TYPES = Object.freeze({
  MOVE: 'move',
});
export const GAME_OBJECT_TYPES = Object.freeze({
  FOOD: 'food',
  AGENT: 'agent',
});
export const TICK_MS = 100;
export const WORLD_WIDTH = 800;
export const WOLRD_HEIGHT = 600;
export const STARTING_FOOD_COUNT = 10;
